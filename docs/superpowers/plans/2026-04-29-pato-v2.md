# Pato V2 Implementation Plan

> **ESTADO: COMPLETADO** — verificado el 2026-09-02 contra el código, no contra estas
> casillas. Los 4 cambios y las 12 tareas están implementados y en `main`.
>
> Comprobado: entidad `ActivityEvent` + repositorio + 3 use cases + 8 referencias en el
> contenedor DI; `CitasModule` con ruta `/citas`, entrada en Sidebar y enlace desde
> LandingPage; 11 llamadas a `addActivityEvent` repartidas en los 6 módulos;
> `HistorialModule` como feed con borrado suave; foto de perfil en `Layout.jsx`;
> `netlify.toml`, `railway.toml` y `nixpacks.toml` eliminados y `vercel.json` creado.
>
> El trabajo que viene **no** es continuación de este plan: está en [docs/ROADMAP.md](../../ROADMAP.md).

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Añadir módulo de Citas dedicado, convertir Historial en feed de actividad con fotos de perfil, mostrar foto de perfil junto al botón de menú, y limpiar archivos de Netlify/Railway a favor de Vercel/Render.

**Architecture:** Clean Architecture existente — nueva entidad `ActivityEvent` con su repositorio localStorage, tres use cases y entrada en el container DI. Los módulos existentes emiten eventos tras cada acción relevante. El Layout carga el perfil "Yo" y muestra su foto junto al hamburger. El HistorialModule se reescribe como feed que consume `getActivityEvents`. El CitasModule es una extracción limpia del código de citas ya existente en HistorialModule.

**Tech Stack:** React 18, Vite 5, Tailwind CSS 3, React Router 6, localStorage (sin tests automatizados — verificación manual en navegador).

---

## Mapa de archivos

**Crear:**
- `src/domain/entities/ActivityEvent.js`
- `src/application/useCases/addActivityEvent.js`
- `src/application/useCases/getActivityEvents.js`
- `src/application/useCases/removeActivityEvent.js`
- `src/infrastructure/storage/localStorageActivityEventRepository.js`
- `src/presentation/pages/CitasModule.jsx`
- `vercel.json`

**Modificar:**
- `src/infrastructure/di/container.js` — registrar ActivityEvent
- `src/presentation/App.jsx` — añadir ruta `/citas`
- `src/presentation/components/Sidebar.jsx` — añadir entrada Citas
- `src/presentation/components/Layout.jsx` — foto de perfil junto al menú
- `src/presentation/pages/HistorialModule.jsx` — reescribir como feed
- `src/presentation/pages/LandingPage.jsx` — link sección → /citas
- `src/presentation/pages/CitasModule.jsx` — instrumentar con addActivityEvent
- `src/presentation/pages/LettersModule.jsx` — emitir evento al enviar carta
- `src/presentation/pages/SettingsModule.jsx` — emitir eventos al guardar cambios
- `src/presentation/pages/MediaModule.jsx` — emitir eventos al añadir/eliminar
- `src/presentation/pages/PartnerProfileModule.jsx` — emitir evento al guardar + custom event
- `src/presentation/pages/PlaylistsModule.jsx` — emitir eventos al añadir/eliminar

**Eliminar:**
- `netlify.toml`
- `server/railway.toml`
- `server/nixpacks.toml`

**Actualizar:**
- `docs/DEPLOY.md`
- `docs/CHECKLIST_PRODUCCION.md`
- `docs/DATOS_PARA_AJUSTES.md`
- `CLAUDE.md`

---

## Task 1: Infraestructura ActivityEvent (entidad + repo + use cases + container)

**Files:**
- Create: `src/domain/entities/ActivityEvent.js`
- Create: `src/infrastructure/storage/localStorageActivityEventRepository.js`
- Create: `src/application/useCases/addActivityEvent.js`
- Create: `src/application/useCases/getActivityEvents.js`
- Create: `src/application/useCases/removeActivityEvent.js`
- Modify: `src/infrastructure/di/container.js`

- [x] **Step 1: Crear entidad ActivityEvent**

```js
// src/domain/entities/ActivityEvent.js
export function createActivityEvent({ id, type, description, profileIndex = 0, createdAt } = {}) {
  return {
    id: id ?? `evt-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`,
    type: type ?? 'generic',
    description: description ?? '',
    profileIndex: profileIndex ?? 0,
    createdAt: createdAt ?? new Date().toISOString(),
  }
}
```

- [x] **Step 2: Crear repositorio localStorage**

```js
// src/infrastructure/storage/localStorageActivityEventRepository.js
const KEY = 'pato_activity_events'

export function createLocalStorageActivityEventRepository() {
  const getAll = async () => {
    try {
      return JSON.parse(localStorage.getItem(KEY) || '[]')
    } catch {
      return []
    }
  }

  const save = async (event) => {
    const all = await getAll()
    const idx = all.findIndex((e) => e.id === event.id)
    if (idx >= 0) all[idx] = event
    else all.push(event)
    localStorage.setItem(KEY, JSON.stringify(all))
  }

  const remove = async (id) => {
    const all = await getAll()
    localStorage.setItem(KEY, JSON.stringify(all.filter((e) => e.id !== id)))
  }

  return { getAll, save, remove }
}
```

- [x] **Step 3: Crear use case addActivityEvent**

```js
// src/application/useCases/addActivityEvent.js
import { createActivityEvent } from '../../domain/entities/ActivityEvent.js'

export const addActivityEvent = (repo) => async (eventData) => {
  const event = createActivityEvent(eventData)
  await repo.save(event)
  return event
}
```

- [x] **Step 4: Crear use case getActivityEvents**

```js
// src/application/useCases/getActivityEvents.js
export const getActivityEvents = (repo) => async () => {
  const events = await repo.getAll()
  return [...events].sort((a, b) => new Date(b.createdAt) - new Date(a.createdAt))
}
```

- [x] **Step 5: Crear use case removeActivityEvent**

```js
// src/application/useCases/removeActivityEvent.js
export const removeActivityEvent = (repo) => async (id) => {
  await repo.remove(id)
}
```

- [x] **Step 6: Registrar en container.js**

Añadir al inicio de `src/infrastructure/di/container.js` (después de los imports existentes):

```js
import { createLocalStorageActivityEventRepository } from '../storage/localStorageActivityEventRepository.js'
import { addActivityEvent } from '../../application/useCases/addActivityEvent.js'
import { getActivityEvents } from '../../application/useCases/getActivityEvents.js'
import { removeActivityEvent } from '../../application/useCases/removeActivityEvent.js'
```

Añadir al final de los `const repo = ...`:

```js
const activityEventRepo = createLocalStorageActivityEventRepository()
```

Añadir al objeto `container`:

```js
  addActivityEvent: addActivityEvent(activityEventRepo),
  getActivityEvents: getActivityEvents(activityEventRepo),
  removeActivityEvent: removeActivityEvent(activityEventRepo),
```

- [x] **Step 7: Verificar — abrir la app en el navegador**

Ejecutar `npm run dev` desde la raíz. Abrir DevTools → Console. No debe haber errores de importación. En Console escribir:

```js
const { container } = await import('/src/infrastructure/di/container.js')
await container.addActivityEvent({ type: 'test', description: 'Prueba' })
const evts = await container.getActivityEvents()
console.log(evts)
```

Debe mostrar un array con el evento creado.

- [x] **Step 8: Commit**

```bash
git add src/domain/entities/ActivityEvent.js src/infrastructure/storage/localStorageActivityEventRepository.js src/application/useCases/addActivityEvent.js src/application/useCases/getActivityEvents.js src/application/useCases/removeActivityEvent.js src/infrastructure/di/container.js
git commit -m "feat: add ActivityEvent entity, repo and use cases"
```

---

## Task 2: CitasModule — nueva página, ruta y sidebar

**Files:**
- Create: `src/presentation/pages/CitasModule.jsx`
- Modify: `src/presentation/App.jsx`
- Modify: `src/presentation/components/Sidebar.jsx`

- [x] **Step 1: Crear CitasModule.jsx**

Contenido completo (código extraído de HistorialModule, sin lógica de sentLetters):

```jsx
// src/presentation/pages/CitasModule.jsx
import { useState, useEffect } from 'react'
import { container } from '../../infrastructure/di/container.js'
import { LOGO_DUCK } from '../config/assets.js'

export default function CitasModule() {
  const [citas, setCitas] = useState([])

  const loadCitas = () => container.getCitas().then(setCitas)

  useEffect(() => { loadCitas() }, [])

  return (
    <div className="max-w-3xl mx-auto pt-14 pb-28 px-4">
      <header className="flex items-center gap-3 mb-8">
        <img src={LOGO_DUCK} alt="" className="w-10 h-10 rounded-full object-cover ring-2 ring-pato-coral/40" />
        <h1 className="font-display text-2xl font-semibold text-pato-ink">Citas</h1>
      </header>

      <CitaForm onAdded={loadCitas} />

      <ul className="mt-6 space-y-3">
        {citas.length === 0 ? (
          <li className="rounded-xl px-4 py-5 bg-pato-butter/60 border border-pato-honey/40 text-pato-muted text-sm text-center">
            Aún no hay citas registradas.
          </li>
        ) : (
          citas.map((c) => (
            <CitaCard key={c.id} cita={c} onRemoved={loadCitas} />
          ))
        )}
      </ul>
    </div>
  )
}

function CitaForm({ onAdded }) {
  const [date, setDate] = useState('')
  const [lugar, setLugar] = useState('')
  const [horaEncuentro, setHoraEncuentro] = useState('')
  const [note, setNote] = useState('')

  const handleSubmit = async (e) => {
    e.preventDefault()
    if (!date.trim()) return
    await container.addCita({ date: date.trim(), lugar: lugar.trim(), horaEncuentro: horaEncuentro.trim(), note: note.trim() })
    setDate('')
    setLugar('')
    setHoraEncuentro('')
    setNote('')
    onAdded?.()
  }

  return (
    <form onSubmit={handleSubmit} className="rounded-xl p-4 bg-pato-butter/80 border border-pato-honey/50 space-y-3">
      <div className="grid sm:grid-cols-2 gap-3">
        <div>
          <label className="block text-xs font-medium text-pato-muted mb-1">Fecha de la cita</label>
          <input
            type="date"
            value={date}
            onChange={(e) => setDate(e.target.value)}
            className="w-full rounded-xl border border-pato-honey bg-white/95 px-3 py-2 text-pato-ink text-sm"
          />
        </div>
        <div>
          <label className="block text-xs font-medium text-pato-muted mb-1">Hora de encuentro</label>
          <input
            type="time"
            value={horaEncuentro}
            onChange={(e) => setHoraEncuentro(e.target.value)}
            className="w-full rounded-xl border border-pato-honey bg-white/95 px-3 py-2 text-pato-ink text-sm"
          />
        </div>
      </div>
      <div>
        <label className="block text-xs font-medium text-pato-muted mb-1">Lugar</label>
        <input
          type="text"
          value={lugar}
          onChange={(e) => setLugar(e.target.value)}
          placeholder="Ej. Restaurante El Jardín"
          className="w-full rounded-xl border border-pato-honey bg-white/95 px-3 py-2 text-pato-ink text-sm placeholder-pato-muted"
        />
      </div>
      <div>
        <label className="block text-xs font-medium text-pato-muted mb-1">Nota (opcional)</label>
        <input
          type="text"
          value={note}
          onChange={(e) => setNote(e.target.value)}
          placeholder="Detalles o recuerdo"
          className="w-full rounded-xl border border-pato-honey bg-white/95 px-3 py-2 text-pato-ink text-sm placeholder-pato-muted"
        />
      </div>
      <button
        type="submit"
        className="w-full sm:w-auto px-4 py-2 rounded-xl bg-pato-sage/70 text-pato-ink text-sm font-medium"
      >
        Agregar cita
      </button>
    </form>
  )
}

function CitaCard({ cita, onRemoved }) {
  const remove = async () => {
    if (window.confirm('¿Quitar esta cita?')) {
      await container.removeCita(cita.id)
      onRemoved?.()
    }
  }

  return (
    <li className="rounded-xl px-4 py-4 bg-pato-butter/80 border border-pato-honey/50 flex items-start justify-between gap-3">
      <div className="min-w-0">
        <div className="font-medium text-pato-ink">{formatDate(cita.date)}</div>
        {(cita.lugar || cita.horaEncuentro) && (
          <div className="text-pato-muted text-sm mt-0.5">
            {[cita.lugar, cita.horaEncuentro || null].filter(Boolean).join(' · ')}
          </div>
        )}
        {cita.note && <p className="text-pato-muted text-sm mt-1">{cita.note}</p>}
      </div>
      <button
        type="button"
        onClick={remove}
        className="text-pato-muted text-xs hover:text-pato-coral hover:underline shrink-0"
      >
        Quitar
      </button>
    </li>
  )
}

function formatDate(iso) {
  try {
    return new Date(iso).toLocaleDateString('es', { day: 'numeric', month: 'long', year: 'numeric' })
  } catch {
    return iso
  }
}
```

- [x] **Step 2: Añadir ruta /citas en App.jsx**

En `src/presentation/App.jsx`, añadir después del import de `HistorialModule`:

```js
const CitasModule = lazy(() => import('./pages/CitasModule'))
```

Y dentro de `<Routes>`, añadir antes de la ruta de historial:

```jsx
<Route path="/citas" element={<CitasModule />} />
```

- [x] **Step 3: Actualizar Sidebar.jsx**

Reemplazar el array `nav` completo en `src/presentation/components/Sidebar.jsx`:

```js
const nav = [
  { path: '/', label: 'Inicio', duck: DUCK_IMAGES[0] },
  { path: '/citas', label: 'Citas', duck: DUCK_IMAGES[2] },
  { path: '/fotos', label: 'Fotos y videos', duck: DUCK_IMAGES[1] },
  { path: '/perfil-pareja', label: 'Perfil de la pareja', duck: DUCK_IMAGES[0] },
  { path: '/playlists', label: 'Playlists', duck: DUCK_IMAGES[1] },
  { path: '/cartas', label: 'Cartas', duck: DUCK_IMAGES[2] },
  { path: '/configuracion', label: 'Configuración', duck: DUCK_IMAGES[3] },
  { path: '/historial', label: 'Historial', duck: DUCK_IMAGES[4] },
]
```

- [x] **Step 4: Verificar en navegador**

Abrir `http://localhost:5173`. El sidebar debe tener "Citas" entre Inicio y Fotos. Navegar a `/citas` — debe cargar el formulario y lista de citas existentes.

- [x] **Step 5: Commit**

```bash
git add src/presentation/pages/CitasModule.jsx src/presentation/App.jsx src/presentation/components/Sidebar.jsx
git commit -m "feat: add CitasModule at /citas with sidebar entry"
```

---

## Task 3: Instrumentar CitasModule con addActivityEvent

**Files:**
- Modify: `src/presentation/pages/CitasModule.jsx`

- [x] **Step 1: Añadir evento al agregar cita**

En `CitaForm.handleSubmit`, después de `await container.addCita(...)` y antes de limpiar el formulario:

```js
await container.addActivityEvent({
  type: 'cita_added',
  description: `Agregó una nueva cita: ${formatDateShort(date.trim())}${lugar.trim() ? ` en ${lugar.trim()}` : ''}`,
})
```

Añadir la función `formatDateShort` al final del archivo (antes del último cierre):

```js
function formatDateShort(iso) {
  try {
    return new Date(iso).toLocaleDateString('es', { day: 'numeric', month: 'short', year: 'numeric' })
  } catch {
    return iso
  }
}
```

- [x] **Step 2: Añadir evento al quitar cita**

En `CitaCard.remove`, después de `await container.removeCita(cita.id)`:

```js
await container.addActivityEvent({
  type: 'cita_removed',
  description: `Quitó la cita del ${formatDateShort(cita.date)}${cita.lugar ? ` en ${cita.lugar}` : ''}`,
})
```

Nota: `formatDateShort` ya está disponible en el mismo archivo desde el step anterior.

- [x] **Step 3: Verificar**

Agregar una cita en `/citas`. Navegar a `/historial` (aún con el Historial viejo — en el siguiente task se reescribe). Verificar en DevTools → Application → localStorage que existe `pato_activity_events` con el evento `cita_added`.

- [x] **Step 4: Commit**

```bash
git add src/presentation/pages/CitasModule.jsx
git commit -m "feat: emit ActivityEvent on cita add/remove"
```

---

## Task 4: Instrumentar LettersModule

**Files:**
- Modify: `src/presentation/pages/LettersModule.jsx`

- [x] **Step 1: Emitir evento en sendByMailto**

En `LettersModule.sendByMailto`, después de `await container.logSentLetter(letter)`:

```js
await container.addActivityEvent({
  type: 'letter_sent',
  description: `Envió una carta: "${letter.subject || '(Sin asunto)'}"`,
})
```

- [x] **Step 2: Emitir evento en sendByGmail**

En `LettersModule.sendByGmail`, dentro del bloque `if (result.ok)`, después de `await container.logSentLetter(letter)`:

```js
await container.addActivityEvent({
  type: 'letter_sent',
  description: `Envió una carta por Gmail: "${letter.subject || '(Sin asunto)'}"`,
})
```

- [x] **Step 3: Verificar**

En `/cartas`, enviar una carta por mailto. Verificar en localStorage → `pato_activity_events` que aparece el evento `letter_sent`.

- [x] **Step 4: Commit**

```bash
git add src/presentation/pages/LettersModule.jsx
git commit -m "feat: emit ActivityEvent on letter sent"
```

---

## Task 5: Instrumentar SettingsModule

**Files:**
- Modify: `src/presentation/pages/SettingsModule.jsx`

- [x] **Step 1: Emitir eventos en handleSave**

Reemplazar `handleSave` completo por esta versión que detecta qué cambió:

```js
const handleSave = async () => {
  const prevMetSince = state.metSince?.slice(0, 10) ?? ''
  const prevStatusId = state.currentRelationshipStatusId ?? ''

  await update({
    metSince: metSince || new Date().toISOString().slice(0, 10),
    currentRelationshipStatusId: currentStatusId,
    relationshipStatuses: statuses,
  })

  if (metSince !== prevMetSince) {
    await container.addActivityEvent({
      type: 'date_changed',
      description: `Cambió la fecha de inicio de la relación a ${formatDateShort(metSince)}`,
    })
  }

  if (currentStatusId !== prevStatusId && currentStatusId) {
    const statusLabel = statuses.find((s) => s.id === currentStatusId)?.label ?? currentStatusId
    await container.addActivityEvent({
      type: 'status_changed',
      description: `Actualizó el estado de la relación a "${statusLabel}"`,
    })
  }

  setSaved(true)
  setTimeout(() => setSaved(false), 2000)
}
```

- [x] **Step 2: Añadir imports y helper**

Añadir al inicio del archivo, después de los imports existentes:

```js
import { container } from '../../infrastructure/di/container.js'
```

Añadir la función helper al final del archivo:

```js
function formatDateShort(iso) {
  try {
    return new Date(iso).toLocaleDateString('es', { day: 'numeric', month: 'long', year: 'numeric' })
  } catch {
    return iso
  }
}
```

- [x] **Step 3: Verificar**

En `/configuracion`, cambiar el estado de la relación y guardar. Verificar en localStorage → `pato_activity_events` que aparece el evento `status_changed`.

- [x] **Step 4: Commit**

```bash
git add src/presentation/pages/SettingsModule.jsx
git commit -m "feat: emit ActivityEvent on settings date/status change"
```

---

## Task 6: Instrumentar MediaModule

**Files:**
- Modify: `src/presentation/pages/MediaModule.jsx`

- [x] **Step 1: Emitir evento al añadir media**

En `MediaModule.handleFileChange`, dentro del loop `for (const file of ...)`, después de `await container.addMedia(...)`:

```js
await container.addActivityEvent({
  type: 'media_added',
  description: `Agregó ${type === 'video' ? 'un video' : 'una foto'} al álbum`,
})
```

- [x] **Step 2: Emitir evento al eliminar media**

En `MediaModule.handleDelete`, dentro del `if (window.confirm(...))`, después de `await container.deleteMedia(id)`:

```js
await container.addActivityEvent({
  type: 'media_removed',
  description: 'Eliminó un archivo del álbum',
})
```

- [x] **Step 3: Añadir import de container si no existe**

Verificar que el archivo ya tiene `import { container } from '../../infrastructure/di/container.js'` — ya existe, no añadir de nuevo.

- [x] **Step 4: Verificar**

En `/fotos`, subir una foto. Verificar en localStorage → `pato_activity_events` que aparece el evento `media_added`.

- [x] **Step 5: Commit**

```bash
git add src/presentation/pages/MediaModule.jsx
git commit -m "feat: emit ActivityEvent on media add/remove"
```

---

## Task 7: Instrumentar PartnerProfileModule

**Files:**
- Modify: `src/presentation/pages/PartnerProfileModule.jsx`

- [x] **Step 1: Emitir evento en handleSave**

En `PartnerProfileModule.handleSave`, después de `setProfiles(await container.getPartnerProfiles())`:

```js
const label = index === 0 ? 'Yo' : 'Pareja'
await container.addActivityEvent({
  type: 'profile_updated',
  description: `Actualizó el perfil de ${label}`,
})
window.dispatchEvent(new Event('pato:profile-updated'))
```

- [x] **Step 2: Verificar**

En `/perfil-pareja`, guardar el perfil "Yo". Verificar en localStorage → `pato_activity_events` que aparece el evento `profile_updated`.

- [x] **Step 3: Commit**

```bash
git add src/presentation/pages/PartnerProfileModule.jsx
git commit -m "feat: emit ActivityEvent and custom event on profile save"
```

---

## Task 8: Instrumentar PlaylistsModule

**Files:**
- Modify: `src/presentation/pages/PlaylistsModule.jsx`

- [x] **Step 1: Emitir evento al agregar playlist**

En `PlaylistsModule.handleAdd`, después de `setUrl('')` y antes de `setPlaylists(...)`:

```js
await container.addActivityEvent({
  type: 'playlist_added',
  description: `Agregó la playlist "${result.name ?? 'Playlist'}" de ${result.platform === 'spotify' ? 'Spotify' : 'YouTube'}`,
})
```

- [x] **Step 2: Emitir evento al quitar playlist**

En `PlaylistsModule.handleRemove`, después de `await container.removePlaylist(id)`:

```js
await container.addActivityEvent({
  type: 'playlist_removed',
  description: 'Eliminó una playlist',
})
```

- [x] **Step 3: Verificar**

En `/playlists`, agregar una playlist. Verificar en localStorage → `pato_activity_events` que aparece el evento `playlist_added`.

- [x] **Step 4: Commit**

```bash
git add src/presentation/pages/PlaylistsModule.jsx
git commit -m "feat: emit ActivityEvent on playlist add/remove"
```

---

## Task 9: Reescribir HistorialModule como feed de actividad

**Files:**
- Modify: `src/presentation/pages/HistorialModule.jsx`

- [x] **Step 1: Reemplazar HistorialModule.jsx completo**

```jsx
// src/presentation/pages/HistorialModule.jsx
import { useState, useEffect } from 'react'
import { container } from '../../infrastructure/di/container.js'
import { LOGO_DUCK } from '../config/assets.js'

export default function HistorialModule() {
  const [events, setEvents] = useState([])
  const [profiles, setProfiles] = useState([null, null])

  const load = async () => {
    const [evts, profs] = await Promise.all([
      container.getActivityEvents(),
      container.getPartnerProfiles(),
    ])
    setEvents(evts)
    setProfiles(profs)
  }

  useEffect(() => { load() }, [])

  return (
    <div className="max-w-3xl mx-auto pt-14 pb-28 px-4">
      <header className="flex items-center gap-3 mb-8">
        <img src={LOGO_DUCK} alt="" className="w-10 h-10 rounded-full object-cover ring-2 ring-pato-coral/40" />
        <h1 className="font-display text-2xl font-semibold text-pato-ink">Historial</h1>
        <span className="text-pato-muted text-sm">· Actividad de la app</span>
      </header>

      {events.length === 0 ? (
        <div className="rounded-xl px-4 py-10 bg-pato-butter/60 border border-pato-honey/40 text-pato-muted text-sm text-center">
          Aún no hay actividad registrada. Las acciones que realicen en la app irán apareciendo aquí.
        </div>
      ) : (
        <ul className="space-y-3">
          {events.map((evt) => (
            <ActivityEventCard
              key={evt.id}
              event={evt}
              profiles={profiles}
              onRemoved={load}
            />
          ))}
        </ul>
      )}
    </div>
  )
}

function ActivityEventCard({ event, profiles, onRemoved }) {
  const profile = Array.isArray(profiles) ? (profiles[event.profileIndex] ?? null) : null
  const photoUrl = profile?.profilePhotoUrl || null
  const initial = profile?.nombre?.[0]?.toUpperCase() ?? '?'

  const remove = async () => {
    if (window.confirm('¿Quitar este evento del historial?')) {
      await container.removeActivityEvent(event.id)
      onRemoved?.()
    }
  }

  return (
    <li className="rounded-xl px-4 py-3 bg-pato-butter/80 border border-pato-honey/50 flex items-start gap-3">
      <div className="shrink-0 w-9 h-9 rounded-full overflow-hidden border border-pato-honey/40 bg-pato-peach/60 flex items-center justify-center">
        {photoUrl ? (
          <img src={photoUrl} alt="" className="w-full h-full object-cover" />
        ) : (
          <span className="text-sm font-medium text-pato-ink select-none">{initial}</span>
        )}
      </div>

      <div className="flex-1 min-w-0">
        <p className="text-sm text-pato-ink leading-snug">{event.description}</p>
        <time className="text-xs text-pato-muted mt-0.5 block">{formatRelativeTime(event.createdAt)}</time>
      </div>

      <button
        type="button"
        onClick={remove}
        className="shrink-0 text-pato-muted text-xs hover:text-pato-coral hover:underline mt-0.5"
      >
        Quitar
      </button>
    </li>
  )
}

function formatRelativeTime(iso) {
  try {
    const d = new Date(iso)
    const diff = Date.now() - d.getTime()
    const mins = Math.floor(diff / 60000)
    if (mins < 1) return 'Ahora mismo'
    if (mins < 60) return `Hace ${mins} min`
    const hours = Math.floor(mins / 60)
    if (hours < 24) return `Hace ${hours} h`
    const days = Math.floor(hours / 24)
    if (days < 7) return `Hace ${days} día${days > 1 ? 's' : ''}`
    return d.toLocaleDateString('es', { day: 'numeric', month: 'short', year: 'numeric' })
  } catch {
    return iso
  }
}
```

- [x] **Step 2: Verificar**

Navegar a `/historial`. Debe mostrar los eventos generados en los tasks anteriores (si se probaron). Si el feed está vacío, realizar una acción en la app (agregar cita, guardar perfil) y volver. Verificar que la foto de perfil aparece en cada tarjeta si está configurada.

- [x] **Step 3: Commit**

```bash
git add src/presentation/pages/HistorialModule.jsx
git commit -m "feat: rewrite HistorialModule as ActivityEvent feed"
```

---

## Task 10: Layout — foto de perfil junto al botón de menú

**Files:**
- Modify: `src/presentation/components/Layout.jsx`

- [x] **Step 1: Reemplazar Layout.jsx completo**

```jsx
// src/presentation/components/Layout.jsx
import { useState, useEffect } from 'react'
import { useLocation } from 'react-router-dom'
import { Sidebar } from './Sidebar'
import { NowPlayingWidget } from './NowPlayingWidget'
import { container } from '../../infrastructure/di/container.js'

export function Layout({ children }) {
  const [sidebarOpen, setSidebarOpen] = useState(false)
  const [profilePhotoUrl, setProfilePhotoUrl] = useState(null)
  const location = useLocation()

  useEffect(() => {
    const refresh = () => {
      container.getPartnerProfiles().then((p) => {
        setProfilePhotoUrl(p?.[0]?.profilePhotoUrl || null)
      })
    }
    refresh()
    window.addEventListener('pato:profile-updated', refresh)
    return () => window.removeEventListener('pato:profile-updated', refresh)
  }, [])

  return (
    <div className="min-h-[100dvh] bg-pato-cream flex">
      <Sidebar open={sidebarOpen} onClose={() => setSidebarOpen(false)} currentPath={location.pathname} currentHash={location.hash} />
      <main
        className="flex-1 relative min-w-0 pl-14 sm:pl-16"
        style={{
          paddingTop: 'max(3.5rem, env(safe-area-inset-top, 0px))',
          paddingBottom: 'max(5rem, env(safe-area-inset-bottom, 0px))',
        }}
      >
        <div
          className="fixed z-30 flex items-center gap-2"
          style={{ left: '0.75rem', top: 'max(1rem, env(safe-area-inset-top))' }}
        >
          <button
            type="button"
            onClick={() => setSidebarOpen(true)}
            className="p-3 min-w-[44px] min-h-[44px] rounded-xl bg-pato-butter shadow-md border border-pato-honey/60 text-pato-ink hover:bg-pato-peach hover:border-pato-coral/40 transition-all touch-manipulation"
            aria-label="Abrir menú"
          >
            <MenuIcon />
          </button>
          {profilePhotoUrl && (
            <img
              src={profilePhotoUrl}
              alt="Perfil"
              className="w-8 h-8 rounded-full object-cover border-2 border-pato-honey/60 shadow-sm"
            />
          )}
        </div>
        <NowPlayingWidget />
        {children}
      </main>
    </div>
  )
}

function MenuIcon() {
  return (
    <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 6h16M4 12h16M4 18h16" />
    </svg>
  )
}
```

- [x] **Step 2: Verificar**

Ir a `/perfil-pareja`, poner una foto de perfil y guardar. Volver al Inicio. El círculo con la foto debe aparecer a la derecha del botón hamburguesa. Si la foto ya estaba guardada, aparece al cargar la página directamente.

- [x] **Step 3: Commit**

```bash
git add src/presentation/components/Layout.jsx
git commit -m "feat: show profile photo next to menu button in Layout"
```

---

## Task 11: Actualizar LandingPage — sección Citas

**Files:**
- Modify: `src/presentation/pages/LandingPage.jsx`

- [x] **Step 1: Cambiar sección inferior de Historial a Citas**

Localizar la sección en `LandingPage.jsx` que comienza en:
```jsx
<section className="mt-10 pt-8 border-t border-pato-honey/40">
  <h2 className="font-display text-lg font-semibold text-pato-ink mb-3">Historial</h2>
  <p className="text-sm text-pato-muted mb-3">Citas ya registradas.</p>
```

Reemplazarla completa por:

```jsx
<section className="mt-10 pt-8 border-t border-pato-honey/40">
  <h2 className="font-display text-lg font-semibold text-pato-ink mb-3">Citas</h2>
  {citas.length === 0 ? (
    <p className="text-sm text-pato-muted mb-3">Aún no hay citas registradas.</p>
  ) : (
    <ul className="space-y-3 mb-4">
      {citas.slice(0, 3).map((c) => (
        <li
          key={c.id}
          className="rounded-xl px-4 py-3 bg-pato-butter/80 border border-pato-honey/50 text-pato-ink text-sm"
        >
          <div className="font-medium">{formatDate(c.date)}</div>
          {(c.lugar || c.horaEncuentro) && (
            <div className="text-pato-muted text-xs mt-0.5">
              {[c.lugar, c.horaEncuentro].filter(Boolean).join(' · ')}
            </div>
          )}
          {c.note && <p className="text-pato-muted mt-1 text-xs">{c.note}</p>}
        </li>
      ))}
    </ul>
  )}
  <Link
    to="/citas"
    className="inline-block text-sm text-pato-coral font-medium hover:underline"
  >
    Ver todas las citas →
  </Link>
</section>
```

- [x] **Step 2: Verificar**

En la landing page, la sección inferior debe decir "Citas" y el enlace debe ir a `/citas`. Si hay más de 3 citas, solo muestra las primeras 3.

- [x] **Step 3: Commit**

```bash
git add src/presentation/pages/LandingPage.jsx
git commit -m "feat: update landing section to link to /citas"
```

---

## Task 12: Limpiar Netlify/Railway — agregar Vercel — actualizar docs

**Files:**
- Delete: `netlify.toml`
- Delete: `server/railway.toml`
- Delete: `server/nixpacks.toml`
- Create: `vercel.json`
- Modify: `docs/DEPLOY.md`
- Modify: `docs/CHECKLIST_PRODUCCION.md`
- Modify: `docs/DATOS_PARA_AJUSTES.md`
- Modify: `CLAUDE.md`

- [x] **Step 1: Eliminar archivos de plataforma obsoletos**

```bash
rm netlify.toml server/railway.toml server/nixpacks.toml
```

- [x] **Step 2: Crear vercel.json**

```json
{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
```

- [x] **Step 3: Actualizar DATOS_PARA_AJUSTES.md**

Reemplazar el contenido completo del archivo con esta versión actualizada (mantiene la misma información pero cambia Netlify → Vercel y Railway → Render):

```markdown
# Ajustes Vercel + Render — Pato en producción

Stack de producción:
- **Frontend (Vercel):** conectar repo → Build command `npm run build`, Output `dist`, variable `VITE_API_URL`
- **Backend (Render):** Web Service → Root `server/`, Start `npm start`, variables de entorno en el panel

---

## Vercel — Frontend

### Configuración de build
- **Build command:** `npm run build`
- **Output directory:** `dist`
- **Install command:** `npm install`

### Variable de entorno obligatoria
En Vercel → tu proyecto → **Settings → Environment Variables**:

| Variable | Valor |
|----------|-------|
| `VITE_API_URL` | URL pública del backend en Render (ej. `https://pato-api.onrender.com`) sin barra final |

Después de añadir o cambiar la variable → **Deployments → Redeploy** para que el build la incluya.

### SPA (React Router)
El `vercel.json` en la raíz del repo ya configura el rewrite necesario para que todas las rutas sirvan `index.html`.

---

## Render — Backend

### Configuración del servicio
- **Type:** Web Service
- **Root directory:** `server`
- **Build command:** `npm install`
- **Start command:** `npm start`

### Variables de entorno en Render
En el servicio → **Environment**:

| Variable | Uso |
|----------|-----|
| `FRONTEND_URL` | URL del frontend en Vercel (ej. `https://pato.vercel.app`) |
| `SPOTIFY_REDIRECT_URI` | `https://tu-api.onrender.com/api/spotify/callback` |
| `YOUTUBE_REDIRECT_URI` | `https://tu-api.onrender.com/api/youtube/callback` |
| `GMAIL_USER` | Correo Gmail |
| `GMAIL_APP_PASSWORD` | Contraseña de aplicación Gmail |
| `SPOTIFY_CLIENT_ID` | Desde Spotify for Developers |
| `SPOTIFY_CLIENT_SECRET` | Desde Spotify for Developers |
| `YOUTUBE_CLIENT_ID` | Desde Google Cloud Console |
| `YOUTUBE_CLIENT_SECRET` | Desde Google Cloud Console |
| `YOUTUBE_API_KEY` | Desde Google Cloud Console |

### Health check
En Render → servicio → **Settings → Health Check Path**: `/api/health`

---

## OAuth (Spotify y Google) en producción

### Spotify Dashboard
Añadir en Redirect URIs: `https://tu-api.onrender.com/api/spotify/callback`

### Google Cloud Console
En credenciales OAuth → URIs de redirección: `https://tu-api.onrender.com/api/youtube/callback`

---

## Resumen de variables por servicio

| Variable | Dónde se configura | Uso |
|----------|--------------------|-----|
| `VITE_API_URL` | Vercel (Environment Variables) | URL del backend; se inyecta en build time |
| `FRONTEND_URL` | Render (Environment) | URL del frontend; usada por backend en redirects OAuth |
| `SPOTIFY_REDIRECT_URI` | Render | Callback OAuth Spotify |
| `YOUTUBE_REDIRECT_URI` | Render | Callback OAuth YouTube |
| `GMAIL_USER`, `GMAIL_APP_PASSWORD` | Render | Envío de cartas |
| `SPOTIFY_CLIENT_ID`, `SPOTIFY_CLIENT_SECRET` | Render | Ahora suena y playlists |
| `YOUTUBE_CLIENT_ID`, `YOUTUBE_CLIENT_SECRET`, `YOUTUBE_API_KEY` | Render | YouTube playlists y OAuth |
```

- [x] **Step 4: Actualizar CHECKLIST_PRODUCCION.md**

Reemplazar el bloque que menciona Netlify/Cloudflare Pages en la sección "## 3. Frontend" por:

```markdown
## 3. Frontend: Vercel

La variable **`VITE_API_URL`** debe estar definida en Vercel y ser **`https://tu-api.onrender.com`** (sin barra final). Después de añadirla o cambiarla, hay que **volver a desplegar**.

### Vercel
- **Build:** Connect to Git → **Build command** = `npm run build`, **Output directory** = `dist`.
- **Variables:** En el proyecto → **Settings → Environment variables** → añadir `VITE_API_URL` con la URL del backend en Render.
- **SPA:** El `vercel.json` en la raíz del repo ya maneja las redirecciones para React Router.
- **Después de cambiar la variable:** **Deployments → Redeploy**.

### Comprobar que el backend responde
Abrir en el navegador: `https://tu-api.onrender.com/api/health`. Debe devolver `{"ok":true}`.
```

Y reemplazar cualquier referencia a Railway por Render en el resto del documento (backend, variables, logs).

- [x] **Step 5: Actualizar DEPLOY.md**

Reemplazar la sección "### Opción A — Recomendada para empezar (gratis)" por:

```markdown
### Opción A — Recomendada (gratis)

- **Frontend:** **Vercel**
  - Dominio gratis: `pato.vercel.app`.
  - Conectar el repo de GitHub; despliega en cada push a `main`.
  - `vercel.json` en la raíz ya maneja el routing SPA.
- **Backend:** **Render**
  - Web Service, root `server/`, start `npm start`.
  - Dominio tipo `pato-api.onrender.com`.
  - Define todas las variables de entorno en el panel.
```

Y en la tabla de "Parte / Qué es / Dónde desplegarla" actualizar Vercel/Netlify → Vercel y Railway/Render → Render.

- [x] **Step 6: Actualizar CLAUDE.md del proyecto pato**

En `CLAUDE.md` (raíz del proyecto pato), en la sección de Deployment, reemplazar la tabla por:

```markdown
| Part | Platform | Notes |
|---|---|---|
| Frontend | Vercel | `npm run build`, publish `dist/`, `vercel.json` para SPA routing, set `VITE_API_URL` env var |
| Backend | Render | root `server/`, start `npm start`, set all env vars |
| Database | Supabase | reservado para futura integración |
```

Y eliminar la nota sobre `netlify.toml`.

- [x] **Step 7: Verificar**

```bash
git status
```

Debe mostrar como eliminados: `netlify.toml`, `server/railway.toml`, `server/nixpacks.toml`. Como nuevos: `vercel.json`. Como modificados: los 4 docs.

- [x] **Step 8: Commit**

```bash
git add -A
git commit -m "chore: remove Netlify/Railway files, add vercel.json, update docs for Vercel+Render"
```

---

## Verificación final end-to-end

> **Las casillas siguientes siguen sin marcar a propósito.** Son verificación manual en
> navegador y requieren una persona delante: nadie las ha ejecutado todavía. El código
> está implementado y compila, pero el comportamiento en pantalla no se ha comprobado.

- [ ] Ejecutar `npm run dev:all` (frontend + backend juntos)
- [ ] En `/citas`: agregar una cita → aparece en la lista y el contador en Inicio aumenta
- [ ] En `/citas`: quitar una cita
- [ ] En `/historial`: los dos eventos (cita_added y cita_removed) aparecen con foto de perfil y timestamp
- [ ] En `/perfil-pareja`: guardar foto de perfil → aparece el círculo junto al hamburger en todas las páginas sin recargar
- [ ] En `/configuracion`: cambiar estado de la relación y guardar → aparece en Historial
- [x] `npm run build` sin errores — verificado 2026-09-02 (98 módulos, sin errores)
